[package]
name = "terminal-model"
version = "0.1.0"
edition = "2024"

[dependencies]
embedded-graphics = "0.8.1"
profont = "0.7.0"
vte = { version = "0.11", default-features = false, features = ["no_std"] }
